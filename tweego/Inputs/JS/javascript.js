// Configure such that audio does not pause on volume becoming 0 
Config.audio.pauseOnFadeToZero = false;

// Example running a function upon passage display.
$(document).on(':passagedisplay', function (ev) {
    // setTime();
});